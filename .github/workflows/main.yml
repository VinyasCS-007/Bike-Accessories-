name: CI/CD Pipeline

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  # Stage 1: Backend CI
  backend-ci:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./server

    steps:
      - uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: "18"
          cache: "npm"
          cache-dependency-path: server/package-lock.json

      - name: Install Dependencies
        run: npm ci

      - name: Check for Syntax Errors
        run: node --check server.js

  # Stage 2: Frontend CI
  frontend-ci:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./client

    steps:
      - uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: "18"
          cache: "npm"
          cache-dependency-path: client/package-lock.json

      - name: Install Dependencies
        run: npm ci

      - name: Build Frontend
        run: npm run build

  # Stage 3: Database CI
  database-ci:
    runs-on: ubuntu-latest
    services:
      mysql:
        image: mysql:8.0
        env:
          MYSQL_ROOT_PASSWORD: root
          MYSQL_DATABASE: bike_accessories
        ports:
          - 3306:3306
        options: >-
          --health-cmd="mysqladmin ping"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=3

    defaults:
      run:
        working-directory: ./server

    steps:
      - uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: "18"
          cache: "npm"
          cache-dependency-path: server/package-lock.json

      - name: Install Dependencies
        run: npm ci

      - name: Wait for MySQL to be ready
        run: |
          until mysqladmin ping -h 127.0.0.1 -u root --password=root --silent; do
            echo "Waiting for MySQL..."
            sleep 5
          done

      - name: Test Database Initialization
        env:
          DB_HOST: 127.0.0.1
          DB_USER: root
          DB_PASSWORD: root
          DB_NAME: bike_accessories
        run: node init-db.js

  # Stage 4: Docker Integration (Build & Test Containers)
  docker-integration:
    needs: [backend-ci, frontend-ci, database-ci]
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Build Docker Images
        run: docker-compose build

      - name: Verify Containers Start
        run: |
          docker-compose up -d
          sleep 10
          docker-compose ps
          docker-compose logs server
          docker-compose down

  # Stage 4: Publish Images (Optional - Requires Secrets)
  # This stage is commented out. To enable, add DOCKERHUB_USERNAME and DOCKERHUB_TOKEN to repo secrets.
  # publish-images:
  #   needs: docker-integration
  #   runs-on: ubuntu-latest
  #   if: github.ref == 'refs/heads/main'
  #
  #   steps:
  #   - uses: actions/checkout@v3
  #
  #   - name: Login to Docker Hub
  #     uses: docker/login-action@v2
  #     with:
  #       username: ${{ secrets.DOCKERHUB_USERNAME }}
  #       password: ${{ secrets.DOCKERHUB_TOKEN }}
  #
  #   - name: Build and Push Server
  #     uses: docker/build-push-action@v4
  #     with:
  #       context: ./server
  #       push: true
  #       tags: ${{ secrets.DOCKERHUB_USERNAME }}/bike-acc-server:latest
  #
  #   - name: Build and Push Client
  #     uses: docker/build-push-action@v4
  #     with:
  #       context: ./client
  #       push: true
  #       tags: ${{ secrets.DOCKERHUB_USERNAME }}/bike-acc-client:latest
